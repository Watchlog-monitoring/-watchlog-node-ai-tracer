{
  "name": "@watchlog/ai-tracer",
  "version": "1.1.1",
  "description": "`watchlog-ai-tracer` is a lightweight SDK for tracing AI interactions (like GPT-4 requests) by sending spans to the Watchlog Agent installed on your local server.",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^1.11.0",
    "proper-lockfile": "^4.1.2",
    "uuid": "^11.1.0"
  }
}
